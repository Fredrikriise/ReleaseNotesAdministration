@model ReleaseNotesAdministration.ViewModels.ReleaseNoteAdminViewModel

@{
    ViewData["Title"] = "Create release note";
}

<head>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css" integrity="sha384-lZN37f5QGtY3VHgisS14W3ExzMWZxybE1SJSEsQp9S+oqd12jhcu+A56Ebc1zFSJ" crossorigin="anonymous">
</head>

<!--
<script>
    //Client validation
    $(document).ready(function () {
        $(".submitButton").click(function (evt) {
            var TitleValidation = document.getElementById('Title').value;
            var BodyTextValidation =  document.getElementById('BodyText').value;
            var ProductValidation =  document.getElementById('ProductId').value;
            var AuthorValidation = document.getElementById('CreatedBy').value;

            if (TitleValidation.length == 0) {
                evt.preventDefault();
                document.getElementById('TitleVaidation').innerHTML = "Title is required to create a new release note! (This is js validation)";

                $('Title').on('change', function () {
                    var ok = "";
                    $('TitleValidation').html(ok);
                });
            }
        });
    });
</script>
    -->

  <h2>Create release note</h2>
    <hr />
    <a class="btn btn-outline-secondary mb-3" asp-controller="WorkItem" asp-action="ListAllWorkItems"><i class="fa fa-list-ul"></i> Pick work item</a>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="CreateReleaseNote"  onsubmit="return validation()">
                <div class="form-group">
                    <label class="control-label">Title</label>
                    <input asp-for="Title" class="form-control" />
                    <span id="TitleValidation"></span>
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="control-label">Body text</label>
                    <textarea asp-for="BodyText" class="form-control"></textarea>
                    <span asp-validation-for="BodyText" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <!-- Select for å velge produktnavn istedetfor produktid -->
                    @*<select asp-for="ProductId" id="inputState" class="form-control custom-select">
                            <option selected hidden>Choose product..</option>
                        </select>*@

                    <label class="control-label">Product</label>
                    <input asp-for="ProductId" class="form-control" />
                    <span asp-validation-for="ProductId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="control-label">Author</label>
                    <input asp-for="CreatedBy" class="form-control" />
                    <span asp-validation-for="CreatedBy" class="text-danger"></span>
                </div>
                <div class="form-group mb-1">
                    <input type="submit" value="Create" class="btn btn-outline-success mb-0" />
                </div>
                <div>
                    <a class="btn btn-outline-danger " asp-controller="ReleaseNotesAdmin" asp-action="ListAllReleaseNotes"><i class="fa fa-arrow-left"></i> Cancel creation</a>
                </div>
            </form>
        </div>
    </div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
